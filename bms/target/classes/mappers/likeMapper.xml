<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="like">
	<insert id="insertLikePost" parameterType="LikePostDto">
		INSERT INTO
				LIKEPOST(
					MEMBER_ID,
					POST_ID
				)
		VALUES(
			#{memberId},
			#{postId}
		)	
	</insert>
	
	<select id="selectLikePost" parameterType="LikePostDto" resultType="LikePostDto">
		SELECT
				*
		FROM	
				LIKEPOST
		WHERE 
				MEMBER_ID = #{memberId} AND POST_ID = #{postId}
	</select>
	
	<delete id="deleteLikePost" parameterType="LikePostDto">
		DELETE FROM 
				LIKEPOST
		WHERE
				MEMBER_ID = #{memberId} AND POST_ID = #{postId}
	</delete>
	
	<select id="selectLikeCount" parameterType="LikePostDto" resultType="int">
		SELECT
				COUNT(*)
		FROM 
				LIKEPOST
		WHERE
				POST_ID = #{postId}
	</select>
	
	<select id="selectLikePostList" parameterType="String" resultType="PostDto">
		SELECT
				P.MEMBER_ID AS MEMBER_ID,
				P.TITLE	AS TITLE,
				P.CONTENT AS CONTENT,
				P.POST_ID AS POST_ID
		FROM
				POST P 
					 LEFT JOIN LIKEPOST L
					 ON P.POST_ID = L.POST_ID
		WHERE
				L.MEMBER_ID = #{id} 
				
	</select>
	
	<insert id="insertLikeMember" parameterType="LikeMemberDto">
		INSERT INTO
					LIKEMEMBER(
						MEMBER_ID,
						FAVORITE_ID
					)
		VALUES(
			#{memberId},
			#{favoriteId}
		)
	</insert>
</mapper>